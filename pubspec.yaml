name: qr_code_scanner_example
version: 1.0.0+1

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_barcode_scanner: ^2.0.0
  
  path_provider: ^2.0.2  
  barcode_widget: ^2.0.1
  url_launcher: ^6.0.9
  flutter_launcher_icons: ^0.9.1  
flutter_icons:
  android: true
  ios: true
  image_path: "assets/qr2.jpeg"
  cupertino_icons: ^1.0.3


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/  